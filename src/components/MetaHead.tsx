import NextHead from 'next/head';

const MetaHead = () => (
  <NextHead>
    <title>Framer Motion Slideshow</title>
    <meta name="description" content="NextJS" />
  </NextHead>
);

export default MetaHead;
